{
    "name": "pomelo-test",
    "version": "0.1.0",
    "private": true,
    "author": "Rodrigo Medina",
    "dependencies": {
        "axios": "^0.21.1",
        "babel-jest": "^24.8.0",
        "bootstrap": "^4.3.1",
        "cross-storage": "^1.0.0",
        "moment": "^2.24.0",
        "react": "^16.8.6",
        "react-bootstrap": "^1.0.0-beta.12",
        "react-dom": "^16.8.6",
        "react-redux": "^7.1.0",
        "react-router": "^5.0.1",
        "react-router-dom": "^5.0.1",
        "react-scripts": "^3.0.1",
        "redux": "^4.0.4",
        "redux-persist": "^5.10.0"
    },
    "scripts": {
        "open": "start http://localhost:5000",
        "start": "npm i -f && react-scripts start",
        "deploy": "npm install -g serve && npm i -f && jest && react-scripts build  && start http://localhost:5000 && serve -s build",
        "eject": "react-scripts eject",
        "test": "jest"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "@babel/core": "*",
        "@babel/preset-env": "*",
        "@babel/preset-react": "*",
        "serverless-offline": "^5.3.3"
    }
}
